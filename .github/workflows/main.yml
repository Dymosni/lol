name: Generate Podcast Feeds 
on: [push]
jobs:
  build:
    runs-on: ubntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/Checkout@v3
      - name: Run Feed Generator
        uses; Dymosni/podcast-generator@main

        
